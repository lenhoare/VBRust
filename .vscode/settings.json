{
  "// vbr.serverPath": "Optional. Leave unset: the extension auto-finds vbr-lsp/target/release/vbr-lsp(.exe) inside this workspace, so the same repo works on Linux and Windows once you've built the LSP. Set an absolute path only to override.",
  "files.associations": {
    "*.vbr": "vbr"
  }
}
