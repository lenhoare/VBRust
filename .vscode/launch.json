{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "VBR: Debug current file (generated Rust)",
      "preLaunchTask": "VBR: debug build",
      "program": "${fileDirname}/.vbrdebug/${fileBasenameNoExtension}",
      "windows": {
        "program": "${fileDirname}/.vbrdebug/${fileBasenameNoExtension}.exe"
      },
      "args": [],
      "cwd": "${fileDirname}",
      "sourceLanguages": ["rust"]
    }
  ]
}
