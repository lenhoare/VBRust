✘ Format uses a Rust format string, not VB's "#,###.00" patterns. Write Format(x, "{:.2}") for two decimals — that becomes format!("{:.2}", x). The pattern must be a string literal. For grouped thousands, Use num-format.