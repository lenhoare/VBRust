fn vbr_main() -> Result<(), String> {
    println!("hello world!");
    Ok(())
}

fn main() {
    if let Err(error) = vbr_main() {
        eprintln!("Error: {error}");
        std::process::exit(1);
    }
}
