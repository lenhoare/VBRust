ℹ The standard library is a Rust crate — `FileSystem.Read(...)` becomes `FileSystem::read(...)` and `use vbr_stdlib::…;` is added for you.
ℹ Bust names are their lowercase self in Rust — e.g. `Run` becomes `run`. (Functions and variables lowercase, constants uppercase; shown once.)