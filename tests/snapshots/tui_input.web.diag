ℹ VBR names are their lowercase self in Rust — e.g. `Seed` becomes `seed`. (Functions and variables lowercase, constants uppercase; shown once.)
ℹ A `Quit` key has no meaning in a browser (close the tab instead), so that binding is left out of the web build.