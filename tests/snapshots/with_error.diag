✘ [line 3] `With` blocks aren't supported — write the variable name out each time (e.g. `p.x = 1` / `p.y = 2`, not `With p` … `.x = 1`). It's a little more typing but far clearer about what you're touching.