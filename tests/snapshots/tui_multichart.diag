⚠ [line 31] Function 'Linear' is 'linear' on the Rust side — one rule: a VBR name is its lowercase self in Rust.
ℹ VB converts between number types silently; Rust wants it spelled out, so VBR inserts `as` for you. A narrowing conversion (e.g. Long → Integer, or a float to an integer) can lose data.
⚠ [line 40] Function 'Quadratic' is 'quadratic' on the Rust side — one rule: a VBR name is its lowercase self in Rust.