id = "result.is_err"
title = "Is_Err"
category = "Result<T>"
kind = "property"
parent = "result"
anchor = "m/Result.Is_Err"

summary = "Whether a Raw result is a failure."

returns = "Boolean — True for Err, False for Ok."

example = """
Function Never() As Long
    RaiseError "nope"
End Function

Function Main()
    Dim r As Result<Long> = Raw Never()
    Debug.Print r.Is_Err()   ' True
End Function
"""

see_also = ["result", "raw", "result.is_ok"]
