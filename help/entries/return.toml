id = "return"
title = "Return"
category = "Procedures"
kind = "keyword"

syntax = """
Return <value>
Return
"""

summary = "Hands a value back from a Function and stops it."

remarks = """
`Return <value>` ends a `Function` and gives that success value to the caller. Its type must match the function's `As <Type>`. To fail instead, use `RaiseError`.

A bare `Return` (no value) leaves a `Sub` early.

Note: inside a surface **event** handler, a bare `Return` is success (`Ok(())`). An unhandled error ends the event and the app keeps running.
"""

example = """
Function Max(a As Long, b As Long) As Long
    If a > b Then
        Return a
    End If
    Return b
End Function

Function Main()
    Debug.Print Max(3, 9)
End Function
"""

see_also = ["function", "sub", "raiseerror"]
