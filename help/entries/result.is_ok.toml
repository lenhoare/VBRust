id = "result.is_ok"
title = "Is_Ok"
category = "Result<T>"
kind = "property"
parent = "result"
anchor = "m/Result.Is_Ok"

summary = "Whether the result is a success."

returns = "Boolean — True for Ok, False for Err."

example = """
Function Main()
    Dim r As Result<Long> = Ok(5)
    Debug.Print r.Is_Ok()    ' True
End Function
"""

see_also = ["result", "result.is_err", "result.unwrap_or"]
