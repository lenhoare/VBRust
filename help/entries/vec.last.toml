id = "vec.last"
title = "Last"
category = "Vec<T>"
kind = "method"
parent = "vec"
anchor = "m/Vec.Last"

summary = "The last item of the list, safely."

returns = "Option<T> — Some(item) with the final element, or None if the list is empty."

example = """
Function Main()
    Dim v As Vec<Long> = [10, 20, 30]
    Debug.Print v.Last().Unwrap_Or(-1)   ' 30
End Function
"""

see_also = ["vec", "vec.first", "vec.pop", "option"]
