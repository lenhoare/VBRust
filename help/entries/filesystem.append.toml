id = "filesystem.append"
title = "Append"
category = "FileSystem"
kind = "method"
parent = "filesystem"
anchor = "ns/FileSystem.Append"

summary = "Adds text to the end of a file."

arguments = """
path | String | The path of the file to append to.
text | String | The text to add at the end.
"""

returns = "Nothing on success. Fails on error. Creates the file first if it doesn't exist."

example = """
Function Main()
    FileSystem.Write("log.txt", "start")
    FileSystem.Append("log.txt", " more")

    Debug.Print FileSystem.Read("log.txt")   ' start more
End Function
"""

see_also = ["filesystem", "filesystem.write"]
