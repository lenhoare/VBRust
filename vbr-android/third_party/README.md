TinyCC is fetched by `../scripts/fetch-tcc.sh`, not committed.

- `tinycc/` — upstream source (`https://github.com/TinyCC/tinycc`)
- `tcc-host/` — host prefix (`libtcc.a`, `lib/tcc/libtcc1.a`) used by `cargo test`
