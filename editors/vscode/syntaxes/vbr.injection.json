{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "scopeName": "vbr.embedded.injection",
  "comment": "Injects VBR highlighting into `/* vbr … */` block comments inside Rust files (see `vbr embed`). One contiguous region, so source.vbr injects cleanly. `L:source.rust` gives our begin-rule priority over Rust's plain block-comment rule so it claims the region first.",
  "injectionSelector": "L:source.rust -comment.line -string",
  "patterns": [
    {
      "name": "meta.embedded.block.vbr",
      "begin": "/\\*\\s*vbr\\b",
      "beginCaptures": {
        "0": { "name": "comment.block.rust" }
      },
      "end": "\\*/",
      "endCaptures": {
        "0": { "name": "comment.block.rust" }
      },
      "patterns": [{ "include": "source.vbr" }]
    }
  ]
}
