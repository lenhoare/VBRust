' MEANT to fail — no Case Else, so the match isn't exhaustive.
Function Main()
    Dim x As Long = 1
    Select Case x
        Case 1
            Debug.Print "one"
        Case 2
            Debug.Print "two"
    End Select
End Function
