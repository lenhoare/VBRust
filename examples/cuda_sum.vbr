' Parallel Sum on a CudaBuffer — the reduction runs on the GPU.
Function Main()
    Dim xs As Vec<Long> = [1, 2, 3, 4, 5, 6, 7, 8]
    Dim a As CudaBuffer<Long> = CUDA.Upload(xs)
    Dim total As Long = Parallel Sum a
    Debug.Print total
End Function
